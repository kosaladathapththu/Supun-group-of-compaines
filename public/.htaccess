Options -MultiViews
RewriteEngine On
RewriteBase /

# Keep real files/directories available, route everything else to the React app.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
